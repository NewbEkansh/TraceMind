name: mobile
description: TraceMind Mobile Application
version: 1.0.0+1

environment:
  sdk: '>=3.11.0 <4.0.0' # Add this to fix the error you just saw

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  mobile_scanner: ^5.0.0
  geolocator: ^12.0.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true